Function print : void ($text : string)
{
    $o : string;
    o = text;
    $b : string;
    b = o;
    output(0, o);
}

Function main : int (void)
{
    $msg : string;
    msg = "HelloWorld!";
    print(msg);
    return(0);
}